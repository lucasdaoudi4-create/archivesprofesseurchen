node_modules
dist
dist-ssr

# Variables d'environnement — `.env` seul n'attrape pas .env.production,
# .env.development ni .env.staging : le joker est indispensable.
.env*
!.env.example
*.local

# Netlify CLI (state.json, cache de build local)
.netlify/

# Charte graphique 2026 — vit dans un dépôt privé séparé,
# ne doit jamais atterrir sur ce dépôt public.
/charte/

.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
.claude/
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# Masters des visuels — 127 Mo de PNG 8192px fournis par l'auteur.
# Seules les dérivées optimisées de public/img/ sont versionnées (2 Mo).
/visuels/
